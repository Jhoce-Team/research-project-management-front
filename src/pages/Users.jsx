import React from "react";

/**
 * Instrucciones basicas para la interfaz
 * Para esta interfaz se tiene que crear dos estructuras, una para el administrador y otra para el lider.
 * 
 * Para el administrador se debe encontrar una lista de todos los usuarios registrados ya sea si estan autorizados o no,
 * a la hora de ver cada usuario se debe poder visualizar el nickname, gmail, rol y estado(autorizado ; no autorizado). Ademas
 * de eso el administrador podra cambiar el estado del usuario
 * 
 * Para el lider se debe encontrar una lista de todos los estudiantes autorizados y no autorizados y al igual que el administrador podra 
 * cambiar su estado. La unica diferencia entre las dos estructuras es que el lider no puede ver los otros usuarios con rol de
 * administrador o lider.
 */

const Users = () => {
  return <div></div>;
};

export default Users;
